#ifndef _PLAYER_TEXTURE_H
#define _PLAYER_TEXTURE_H

// Represents the textures in the player-spritesheet
enum PlayerTexture {
    TEXTURE1, // I'm not sure what the first 4 do
    TEXTURE2,
    TEXTURE3,
    TEXTURE4,
    WALK1,
    WALK2,
    WALK3,
    WALK4,
    GRADUATION
};

#endif